name: Release desktop builds

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Version tag to build (e.g. v0.4.1). Leave empty to use the pushed tag / create a draft from the current version."
        required: false
        default: ""

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact: RamScoutAI-windows-x64.exe
            optional: false
          - os: macos-14
            artifact: RamScoutAI-macos-arm64.zip
            optional: false
          - os: macos-13
            artifact: RamScoutAI-macos-x64.zip
            optional: true
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.optional }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install -U pip wheel
          # Slim freeze set — skip ultralytics/torch so CI stays fast and artifacts stay downloadable.
          python -m pip install -r requirements-desktop.txt

      - name: Build with PyInstaller
        run: python -m PyInstaller --noconfirm --clean packaging/ramscout.spec

      - name: Package Windows exe
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist/release | Out-Null
          Copy-Item dist/RamScoutAI.exe "dist/release/${{ matrix.artifact }}"

      - name: Package macOS zip
        if: runner.os == 'macOS'
        run: |
          mkdir -p dist/release
          cd dist
          chmod +x RamScoutAI
          zip -9 "release/${{ matrix.artifact }}" RamScoutAI

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/release/${{ matrix.artifact }}
          if-no-files-found: error

  publish:
    needs: build
    if: always() && !cancelled()
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve tag
        id: meta
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" && -n "${{ inputs.tag }}" ]]; then
            TAG="${{ inputs.tag }}"
          elif [[ "${{ github.ref_type }}" == "tag" ]]; then
            TAG="${{ github.ref_name }}"
          else
            TAG="v$(python -c 'from ramscout import __version__; print(__version__)')"
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Flatten assets
        shell: bash
        run: |
          mkdir -p out
          find release-assets -type f -print -exec cp {} out/ \;
          ls -lah out
          # Require at least Windows or one macOS build.
          test -n "$(ls -A out 2>/dev/null)" || (echo "No desktop artifacts were produced" && exit 1)

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          name: RamScoutAI ${{ steps.meta.outputs.tag }}
          draft: false
          generate_release_notes: true
          files: out/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
