# Slim dependency set for Windows/macOS PyInstaller builds (no PyTorch / Ultralytics).
# Verify after install: python -m ramscout.deps --strict
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
httpx>=0.27.0
numpy>=1.26.0
scipy>=1.11.0
opencv-python-headless>=4.10.0
Pillow>=10.0.0
# Required: neural depth + packaged robot ONNX detector (no ultralytics at runtime).
onnxruntime>=1.17.0
yt-dlp[default]>=2025.9.26
openpyxl>=3.1.5
pywebview>=5.0
pyinstaller>=6.10.0
