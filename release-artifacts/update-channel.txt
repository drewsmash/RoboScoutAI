# Default git branch for desktop updates (override per install: RoboScoutAI.exe --channel <branch-or-tag>,
# or env ROBOSCOUT_CHANNEL / RAMSCOUT_GIT_BRANCH).
# The manager reads release-artifacts/manifest.json + RoboScoutAI.exe from this branch.
# App/Setup builds over GitHub's 100 MB git limit are referenced by URL in manifest.json
# and downloaded from GitHub Releases. Legacy 0.5.x updaters still read
# release-artifacts/RoboScoutAI-windows-x64.exe (the thin manager, for migration).
cursor/upload-cookies-github-updater-e0ef
