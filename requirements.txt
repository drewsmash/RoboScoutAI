fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
httpx>=0.27.0
numpy>=1.26.0
scipy>=1.11.0
opencv-python-headless>=4.10.0
Pillow>=10.0.0
yt-dlp[default]>=2025.9.26
yt-dlp-getpot-wpc>=1.1.2
nodriver>=0.40.0
openpyxl>=3.1.5
pytest>=8.3.0
# Required runtime: neural depth (Depth Anything V2 Small) + packaged robot ONNX
# detector path. Weights download separately; see docs/INSTALL.md.
onnxruntime>=1.17.0
# Source-only neural tracker / training export (NOT in desktop freeze):
ultralytics>=8.3.0
# Optional torch/transformers depth backend:
#   pip install -r requirements-depth.txt
# Optional local Laya scout verification:
#   pip install -r requirements-laya.txt
# Verify installs:
#   python -m ramscout.deps --strict
